# NN-example-name

> One-line description of what this demonstrates.

## What it does

A paragraph or two explaining the example in plain terms. Who's it for, what problem does it solve, what's the clever bit.

## Cloudflare features used

- [ ] Pages (static hosting)
- [ ] Pages Functions
- [ ] KV
- [ ] D1
- [ ] R2
- [ ] Durable Objects
- [ ] Queues
- [ ] Workers AI
- [ ] Scheduled Triggers (cron)
- [ ] Turnstile

## Files

```
NN-example-name/
├── README.md       ← this file
├── index.html      ← the page
├── style.css       ← styles (if separate)
├── app.js          ← client JS (if any)
└── functions/      ← Pages Functions, if any
    └── api/
        └── route.ts
```

## Running locally

```bash
# from this folder
wrangler pages dev .
```

## Key files to read

Point readers to the one or two files that actually matter — the Function handler, the interesting CSS, whatever carries the weight of the example.

## Gotchas

Anything that tripped me up, anything someone replicating this should know, anything where the docs aren't quite right.

## What I'd do differently at scale

A sentence or two on how this would change if it weren't a demo — caching strategy, schema trade-offs, etc. Shows I'm thinking past the example.
